//getElementById

const myTitle = document.getElementById('main-title');
 console.log(myTitle);

 //getElementByClassName

 const myContainer=document.getElementsByClassName('container');
  console.log(myContainer);

//class section className

  const myClass=document.getElementsByClassName('section');
  console.log(myClass);

  //getElementByTagName

  const paraNames=document.getElementsByTagName('p');
   console.log( paraNames);

   //querySelector for first button

   const firstButton=document.querySelector('.btn');
   console.log(firstButton);

   //querySelectorAll for all buttons

   const allButtons=document.querySelectorAll('.btn');
   console.log(allButtons);

   //select all p elements within the container

   const pContainer=document.querySelectorAll('.container p');
    console.log(pContainer);